import { PromoCodeValidatorDirective } from './promo-code-validator.directive';

describe('PromoCodeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PromoCodeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
